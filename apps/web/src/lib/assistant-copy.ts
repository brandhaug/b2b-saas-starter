import { m } from '@b2b-saas-starter/i18n/messages'

/** Resolve the provider-light explanation in the current request's language. */
export function assistantUnconfiguredMessage(): string {
  return m.server_assistant_unconfigured()
}
