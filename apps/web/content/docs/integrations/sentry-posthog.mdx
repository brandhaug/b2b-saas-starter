---
title: Sentry and PostHog
description: Env-gated error monitoring and product analytics.
category: integrations
order: 30
tags: [sentry, posthog, observability]
---

Sentry and PostHog activate when configured. Without their variables, neither exports data.

Telemetry export does not depend on either of them: `OTEL_EXPORTER_OTLP_ENDPOINT` ships traces, metrics, and the canonical log records to any OTLP collector, which remains the supported path for wide-event telemetry. Sentry and PostHog add vendor-native error inboxes and product analytics on top.

## Sentry

| Variable     | Where it works                           |
| ------------ | ---------------------------------------- |
| `SENTRY_DSN` | all three workers (web, api, background) |

Server-side, each worker entry is wrapped with the official [`@sentry/cloudflare`](https://docs.sentry.io/platforms/javascript/guides/cloudflare/) SDK (`Sentry.withSentry`). Without a DSN the SDK initializes disabled: no transport is configured, so nothing leaves the worker.

Every failed wide event becomes one Sentry exception, tagged with `service`, `event`, `errorKind`, and the error's `_tag` when it has one, and joined back to the request's OTel trace id. The glue lives in `packages/logger/src/providers.ts`: the same observability seam that owns OTLP export (see `packages/logger/AGENTS.md` and ADR 0050 for the invocation-scoping rules).

In the browser, the web app lazily loads [`@sentry/react`](https://docs.sentry.io/platforms/javascript/guides/react/) only when the root route's loader passes a DSN through (DSNs are public ingest identifiers by design). Client-side errors then report to the same project.

Releases are derived from `SERVICE_VERSION` / `GIT_COMMIT_SHA`; source-map upload is an explicit non-goal for now.

## PostHog

| Variable       | Where it works                                                  |
| -------------- | --------------------------------------------------------------- |
| `POSTHOG_KEY`  | web browser analytics + server-side capture                     |
| `POSTHOG_HOST` | Optional; `.env.example` defaults to `https://us.i.posthog.com` |

Server-side, `packages/logger` posts one event per wide-event scope to PostHog's capture endpoint with `fetch`, keyed by the trace id, with `service`, `status`, and `durationMs` properties. The POST happens inside the invocation that produced the scope: the same invocation-scoped I/O rule ADR 0050 applies to OTLP export.

In the browser, `ClientTelemetry` reads the same root-route loader data and beacons `$pageview` and `$pageleave` to the same endpoint, one per router location. Nothing else is captured and no vendor SDK ships: `navigator.sendBeacon` (with a `keepalive` `fetch` behind it) carries the two events the product needs.
