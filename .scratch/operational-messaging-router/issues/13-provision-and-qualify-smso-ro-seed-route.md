# Provision and Qualify the SMSO.ro Seed Route

Type: task
Status:
Blocked by: 24, 25, 29

## Question

After the console/fake SMS adapter, deployable SMSO.ro adapter, secure callback receiver, reconciliation path, and production-like environment exist, provision the platform-owned SMSO.ro legal account, billing profile, least-access team, capped real-test balance, production API credential, and selected outbound Romania sender; obtain written provider answers for sender approval, API-key rotation, GSM/Unicode segmentation and cost units, debit/refund timing, idempotency and ambiguous submissions, send quotas, callback authentication and retry, status retention, sandbox availability, DPA, subprocessors, retention, security, and incident terms; then execute and record a controlled cross-network encoding, cost, callback, polling, throttling, and terminal-status qualification matrix before the Booking Product launches with Operational Messaging enabled.

## Comments

### Progress — 2026-07-27

Qualification record: [SMSO.ro Seed Route Qualification](../artifacts/smso-ro-seed-route-qualification.md)

The platform account is created. The authenticated UI shows a capped €3.50 / 100-SMS test balance, one existing 40-character API credential, and the common send-only `1XXX` route as purchased with numeric sender ID `4` and €0/year sender cost. Team settings expose configurable delivery callbacks, but the legal billing profile is empty, the team name is unfinished, and the only member is the owner/administrator. No secret value or personal/legal identifier is recorded in the tracker.

At this observation point the ticket was still claimed and could not resolve until the owner supplied the remaining legal/team/test inputs, authorized the provider questionnaire and real sends, the credential and callback had a production target, written provider answers arrived, and the controlled matrix was executed with redacted evidence. The later rescoping below supersedes its development-time timing.

### Rescoped — 2026-07-27

Development will use a console/fake SMS provider and deterministic contract fixtures. No billing profile, paid credit, provider secret, callback registration, support request, or real SMS qualification is needed during development. Live SMSO.ro provisioning and capped real-network qualification move to the pre-production launch gate, after the implementation program produces a deployable adapter, callback receiver, reconciliation path, and production-like environment, but before the Booking Product launches with Operational Messaging enabled. The ticket is unclaimed and blocked by [Synthesize and Seed the Implementation Program](./12-synthesize-and-seed-implementation-program.md) until that program can wire it behind the exact implementation prerequisites.
