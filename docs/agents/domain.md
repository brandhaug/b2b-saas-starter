# Domain Docs

This repository uses a single-context domain-doc layout.

Before exploring or changing the codebase, read the root `CONTEXT.md` and the relevant decisions under `docs/adr/`. Use the glossary's canonical terms and avoid its rejected synonyms. Surface any conflict with an existing ADR explicitly rather than silently overriding it.

`ARCHITECTURE.md` is the system and security map.
