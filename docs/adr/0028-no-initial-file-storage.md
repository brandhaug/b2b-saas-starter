# No initial file storage

The starter does not include R2 or user file upload workflows in the initial scaffold because the workspace, starter module, readiness, API, MCP, reporting, and admin domains do not require file storage. Cloudflare storage extensions can be discussed in educational content, but working upload surfaces are out of scope until a concrete product workflow needs them.
