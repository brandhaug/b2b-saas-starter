# Workspace outbound webhooks

The starter includes outbound webhooks as a workspace module. Workspaces can define webhook endpoints with signing secrets, selected event types, delivery attempts, retry/backoff behavior, and settings UI; inbound provider callbacks remain provider-specific routes rather than a general arbitrary webhook ingestion system.
