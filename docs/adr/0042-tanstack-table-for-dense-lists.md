# TanStack Table for dense lists

The starter uses TanStack Table for dense B2B list surfaces such as admin users, workspace members, API tokens, webhook deliveries, audit events, report history, and integration records. Contributor's table/card visual patterns can guide presentation, but list behavior should stay accessible, paginated or filtered where useful, and consistent across authenticated screens.
