# TanStack Table for dense lists

TanStack Table supplies shared behavior for dense workspace and administration lists. Server-paginated collections retain their canonical ordering; sorting only the loaded page would misrepresent the collection. Simpler displays do not need a table abstraction.
