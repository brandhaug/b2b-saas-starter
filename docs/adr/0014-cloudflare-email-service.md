# Cloudflare Email Service

Cloudflare email bindings send transactional email, matching the deployment platform. React Email owns templates; the dispatcher logs locally when provider configuration is absent. Inbound mail handling is outside the starter scope.
