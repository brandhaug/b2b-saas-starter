# Scripts over CLI

The starter uses focused Bun scripts for setup, environment validation, database migration and seeding, development, testing, linting, formatting, deployment, destruction, and catalog refresh instead of introducing a custom CLI. A CLI is deferred until the starter has multiple generation or customization workflows that justify another abstraction.
