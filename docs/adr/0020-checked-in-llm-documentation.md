# Checked-in LLM documentation

`apps/web/public/llms.txt` and `llms-full.txt` provide a text entry point to the public documentation. They are checked-in files, not build-generated artifacts, and must be updated with the content they describe. Automation should derive them from the content source rather than introduce another metadata inventory.
