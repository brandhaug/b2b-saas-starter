# Build-time syntax highlighting

Shiki highlights MDX code during the build, avoiding browser highlighting code. TypeScript-authored examples use the shared code-block presentation so documentation and public examples retain the same styling without separate client renderers.
