# Env-gated Turnstile

The starter includes Cloudflare Turnstile as an optional provider module for sign-up and sensitive public forms. It should be disabled by default for local development and activated only when Turnstile site key and secret configuration exists.
