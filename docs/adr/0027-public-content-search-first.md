# Public content search first

The command palette searches documentation titles/descriptions, doc categories, and navigation labels without loading article bodies. Workspace and account destinations reuse the sidebar navigation and its permission filters. The showcase landing page contains the FAQ; the palette reaches it through the public navigation entry. Full-text article search and workspace data search remain out of scope; the latter needs authorization, indexing, and freshness rules before it can expose records.
