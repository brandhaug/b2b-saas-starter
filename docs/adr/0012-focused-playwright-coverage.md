# Focused Playwright coverage

The starter includes Playwright from the start with focused coverage for public route rendering, docs/blog/FAQ/search behavior, sign-in rendering, authenticated redirects, and a local seeded workspace smoke path where reliable. Provider-heavy flows such as billing, OAuth, and external integrations should use mocks or narrower tests until real environment configuration is available.
