# Intent node documentation

Small `AGENTS.md` files record architectural constraints at application and package boundaries; `CLAUDE.md` aliases point to the same files. The root indexes these nodes and the domain, architecture, and design documents. Local guidance avoids repeating the whole architecture at every change site.
