# Intent node documentation

The starter uses fresh `AGENTS.md` intent nodes modeled after Hexwardens' structure rather than copied from Hexwardens. The root node points to `CONTEXT.md`, `ARCHITECTURE.md`, and child nodes for the web app, API worker, background worker, database, auth, capabilities, email, and infrastructure so agent guidance matches the starter domain instead of leaking game-specific invariants.
