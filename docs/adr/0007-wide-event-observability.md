# Wide event observability

The starter includes wide event logging from the start across the web, API, and background Cloudflare workers. Request-scoped logging should cover auth, workspace membership, starter module readiness, integration connection attempts, billing state changes, REST and MCP calls, email sends, and catalog refresh runs so operational behavior is visible before features grow around it.
