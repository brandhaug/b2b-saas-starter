{
  "extends": "../config/tsconfig.base.json",
  "compilerOptions": {
    // The test harness spawns the real oxlint binary and writes fixtures to a temp
    // directory, so it needs the Node builtins. Nothing under `src` does.
    "types": ["@types/node"]
  },
  "include": ["src", "test"]
}
