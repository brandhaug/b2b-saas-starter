# Operational Messaging Qualification

This module owns the launch qualification profile and operational alert policy. Gates
are conjunctive and fail closed. Integrity findings are never averaged into SLOs;
Booking and independent email isolation remain explicit qualification evidence.
