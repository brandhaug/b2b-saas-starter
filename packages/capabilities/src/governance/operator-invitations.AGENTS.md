# Operator Invitations

Durable invitation and enrollment lifecycle for dedicated System Operator
identities. The capability authorizes managers from current D1 facts, persists
only invitation and enrollment credential hashes, and never creates an Operator
Session. Operations Auth owns password and factor credential mechanics. The
Operations HTTP boundary owns host-only enrollment cookies and one-time security
setup presentation.
