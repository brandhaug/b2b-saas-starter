# Merchant Catalog Solo launch policy

`solo-launch-policy.ts` is the shared policy vocabulary for Merchant Onboarding and
Merchant Catalog launch behavior. It permits one Owner membership and one active
default Owner-Provider whose profile may be updated; it never enables invitations,
additional roles, or Provider creation.
